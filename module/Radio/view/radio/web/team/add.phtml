<div class="container mt-4">
    <h2><?= $this->member ? 'Editar' : 'Nuevo' ?> Integrante</h2>
    
    <form method="post" enctype="multipart/form-data" class="mt-4">
        <div class="mb-3">
            <label class="form-label">Nombre</label>
            <input type="text" name="name" class="form-control" value="<?= $this->member ? $this->escapeHtmlAttr($this->member['name']) : '' ?>" required>
        </div>
        
        <div class="mb-3">
            <label class="form-label">Rol</label>
            <input type="text" name="role" class="form-control" value="<?= $this->member ? $this->escapeHtmlAttr($this->member['role']) : '' ?>">
        </div>
        
        <div class="mb-3">
            <label class="form-label">Biografía</label>
            <textarea name="bio" class="form-control" rows="4"><?= $this->member ? $this->escapeHtml($this->member['bio']) : '' ?></textarea>
        </div>
        
        <div class="mb-3">
            <label class="form-label">Imagen</label>
            <?php if ($this->member && $this->member['image_url']): ?>
                <div class="mb-2">
                    <img src="<?= $this->member['image_url'] ?>" alt="Current Image" style="height: 100px;">
                </div>
            <?php endif; ?>
            <input type="file" name="image" class="form-control" accept="image/*">
        </div>
        
        <button type="submit" class="btn btn-success">Guardar</button>
        <a href="<?= $this->url('radio-team') ?>" class="btn btn-secondary">Cancelar</a>
    </form>
</div>
