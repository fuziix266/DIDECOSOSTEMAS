<div class="container mt-4">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h2>Noticias</h2>
        <a href="<?= $this->url('radio-news', ['action' => 'add']) ?>" class="btn btn-primary">Nueva Noticia</a>
    </div>

    <table class="table table-striped">
        <thead>
            <tr>
                <th>ID</th>
                <th>Título</th>
                <th>Fecha</th>
                <th>Imagen</th>
                <th>Acciones</th>
            </tr>
        </thead>
        <tbody>
            <?php foreach ($this->news as $item): ?>
            <tr>
                <td><?= $item['id'] ?></td>
                <td><?= $this->escapeHtml($item['title']) ?></td>
                <td><?= $item['published_date'] ?></td>
                <td>
                    <?php if ($item['image_url']): ?>
                        <img src="<?= $item['image_url'] ?>" alt="Img" style="height: 50px;">
                    <?php endif; ?>
                </td>
                <td>
                    <a href="<?= $this->url('radio-news', ['action' => 'edit', 'id' => $item['id']]) ?>" class="btn btn-sm btn-warning">Editar</a>
                    <a href="<?= $this->url('radio-news', ['action' => 'delete', 'id' => $item['id']]) ?>" class="btn btn-sm btn-danger" onclick="return confirm('¿Seguro?')">Eliminar</a>
                </td>
            </tr>
            <?php endforeach; ?>
        </tbody>
    </table>
</div>
